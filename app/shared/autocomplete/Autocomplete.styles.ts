import { StyleSheet } from "react-native";

export const autocompleteStyles = StyleSheet.create({
  container: { flex: 1 },
  input: {
    width: "100%",
    padding: 15,
    backgroundColor: "#fff",
    borderRadius: 115,
    marginTop: 10,
  },
});
